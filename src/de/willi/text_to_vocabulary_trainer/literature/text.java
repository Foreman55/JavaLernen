package de.willi.text_to_vocabulary_trainer.literature;

public class text {

    public static void main(String[] args) {
/*
        // 1. Dateieinlesen in String

        // 2. Bereinigen Zeichen ".,:*(){}[]_-”
        CleanText textArray = new CleanText();
        WordCount words = new WordCount();
        String []cleanedWords =textArray.clean(text.toString());
        WordsResult wordsResult = words.countWord(cleanedWords);

        // Ausgabe
        System.out.println("Es sind insgesamt " + wordsResult.getCountUniqueWords() + " UniqueWords");
        for (UniqueWord uniqueWord: wordsResult.getUniqueWords()){
            System.out.println(uniqueWord.getUniqueName() + " = " + uniqueWord.getWordFrequency());
        }

 */
    }
}
